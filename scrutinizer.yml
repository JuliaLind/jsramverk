build:
  nodes:
    coverage:
      tests:
        override:
            -   command: "NODE_ENV=test nyc --reporter=html --reporter=text --reporter=clover mocha --config mocha.json --exit"
                coverage:
                    file: "backend/coverage/clover.xml"
                    format: "clover"

filter:
    excluded_paths: [frontend-old/*, vue-project/*, vue-project-m/*, backend/coverage/*]
    dependency_paths: [backend/coverage/clover.xml]

checks:
    # ...

coding_style:
    # ...

build_failure_conditions:
    # ...

tools:
    # ...

credentials:
    # ...