checks:
    javascript: true
build:
    nodes:
        coverage:
            tests:
                override:
                    - js-scrutinizer-run
                    coverage:
                        file: backend/coverage/clover.xml
                        format: clover