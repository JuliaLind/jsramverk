checks:
    javascript: true
build:
    tests:
        override:
            # command: - js-scrutinizer-run
            coverage:
                file: backend/coverage/clover.xml
                format: clover